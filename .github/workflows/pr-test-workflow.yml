name: Test Android App

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:
  
jobs:
  pr-test:
    uses: cchanka/github-actions-android-workflows/.github/workflows/android-test-workflow.yml@main